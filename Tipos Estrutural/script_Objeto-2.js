// Object

const person = {
    name: 'John',
    age: 30,
    weight: 88.6,
    isAdmin: true
}
// exibir usando apontamento:
console.log(person.name)

// exibir usando interpolação:
console.log(`${person.name} tem ${person.age} anos`)



